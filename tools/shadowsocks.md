[来源链接](https://blog.phpgao.com/shadowsocks_on_linux.html#编译shadowsocks-libev)

# 在ubuntu的简易搭建
> 
apt-get update
apt-get install -y python-pip
pip install shadowsocks
nohup sserver -k password -p port &